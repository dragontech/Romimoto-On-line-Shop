<% if current_user %>
  <%= link_to t('my_account'), account_path %> [<%= link_to t('logout'), destroy_user_session_path %>]
<% else %>
  <%= t('welcome') %> [<%= link_to t('log_in'), login_path %>]
<% end %>
